import { UnrionLoader } from '@packages/ui/components';

export default function RootLoader() {
  return (
    <div className='contained flex justify-center mt-32'>
      <UnrionLoader />
    </div>
  );
}
